# Preview mode — loaded automatically by `vite --mode preview` (npm run dev:mock).
# Turns on the in-app mock layer (client/src/lib/preview.js) so the entire UI
# runs with sample data and NO backend, database, or API keys.
# Normal `npm run dev` does NOT read this file, so the real API is used as usual.
VITE_PREVIEW=1
